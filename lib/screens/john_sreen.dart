import 'package:flutter/material.dart';

class JohnWidget extends StatelessWidget {
  const JohnWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Hola es es un demos"),
      ),
    );
  }
}