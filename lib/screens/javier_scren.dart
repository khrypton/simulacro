import 'package:flutter/material.dart';

class JavierWidget extends StatelessWidget {
  const JavierWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}